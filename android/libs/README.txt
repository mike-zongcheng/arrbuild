# 将 uni-app 离线 SDK 中的以下文件复制到此目录后，才能编译 GlobalFloat.aar：
#
# - uniapp-v8-release.aar  (或 uniapp-release.aar)
# - fastjson-1.2.83.jar (可选，gradle 会自动下载)
#
# 编译完成后，将生成的 GlobalFloat-release.aar 重命名复制为：
# ../GlobalFloat.aar
